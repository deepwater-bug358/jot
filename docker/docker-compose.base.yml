version: "3"
services:
    app:
        build:
            context: ..
            dockerfile: Dockerfile
        restart: always
        environment:
            PORT: 3000
            DATA_DIR: /app/data
        volumes:
            - ./data/notes:/app/data
        networks:
            - network

networks:
    network:
        driver: bridge
